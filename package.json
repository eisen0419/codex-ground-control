{
  "name": "codex-ground-control",
  "version": "0.1.0",
  "description": "A local-first, fail-closed workflow control plane for Codex.",
  "type": "module",
  "engines": {
    "node": ">=22"
  },
  "bin": {
    "codex-ground-control": "bin/codex-ground-control.js"
  },
  "files": [
    "assets",
    "bin",
    "fixtures",
    "release-lock.json",
    "schemas",
    "src",
    "THIRD_PARTY_NOTICES.md",
    "vendor",
    "LICENSE",
    "README.md"
  ],
  "scripts": {
    "release-lock:generate": "node scripts/release-lock.mjs --write",
    "release-lock:verify": "node scripts/release-lock.mjs",
    "test": "node --test --test-reporter=spec tests/*.test.mjs",
    "test:tracer": "node --test --test-reporter=spec tests/tracer-bullet.test.mjs",
    "typecheck": "node --check bin/codex-ground-control.js && node --check src/cli.js && node --check src/doctor.js && node --check src/global-workflow.js && node --check src/managed-workflow.js && node --check src/project-state.js && node --check src/qualification-contract.js && node --check src/qualification-lab.js && node --check src/safe-files.js && node --check src/workflow-assets.js && node --check src/workflow-error.js"
  },
  "license": "MIT"
}
