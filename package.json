{
  "name": "codex-ground-control",
  "version": "0.1.0",
  "description": "A local-first, fail-closed workflow control plane for Codex.",
  "type": "module",
  "engines": {
    "node": ">=22"
  },
  "bin": {
    "codex-ground-control": "bin/codex-ground-control.js"
  },
  "files": [
    "bin",
    "fixtures",
    "src",
    "LICENSE",
    "README.md"
  ],
  "scripts": {
    "test": "node --test --test-reporter=spec tests/*.test.mjs",
    "test:tracer": "node --test --test-reporter=spec tests/tracer-bullet.test.mjs"
  },
  "license": "MIT"
}
